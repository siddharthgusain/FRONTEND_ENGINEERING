1. Start With A Mock
2. Break The UI Into A Component Hierarchy
3. Build A Static Version in React without any state and props
4. Identify Where Your State Should Live
5. Add Inverse Data Flow(child to parent)
6. Think about adding Routing
7. Add statemanagement if props drilling is killing your code
8. Make components so generic/reusable(single responsibility) so that they can be used in 
another project just by copy pasting.