// Regular expressions are patterns used to match character combinations in "strings"

// Regular expression with literals
const RE = /ab+c/;

// Regular expression with Constructor function of RegExp Object
const RE1 = new RegExp("ab+c")