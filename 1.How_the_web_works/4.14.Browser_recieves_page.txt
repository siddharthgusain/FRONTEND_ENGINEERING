-> In case of HTTP protocol , resource is fetched by Browser 
-> Note , we have talked about http in web , there are other application layer protocols(FTP,SMTP,POP).
-> We are talking about browser perspective which mostly uses HTTP protocol.
-> FTP is used in client softwares like Filezilla, SMTP and POP is used by client software like Thunderbird, Outlook desktop client

-> After getting HTTP response Browser do html , css , js parsing and painting.
-> We will discuss browser internals in next sections.

-> There is one other application layer protocol Websocket(WS)/Websocket secure(WSS) used for bi-directional communication
